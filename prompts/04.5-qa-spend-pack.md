# Prompt 04.5 — QA: Spend + Pack Phase

## Goal
Functionally verify budget + packing end-to-end, fix all defects, close
Phase 04.

## Prerequisite
Segments 04.1–04.4 marked ✅.

## QA procedure (do all)
1. **Automated:** `npm test`, `npm run lint`, `npm run typecheck`,
   `npm run build`.
2. **Budget (browser, both modes):** add/edit/delete expenses; totals + per-
   category + per-day aggregates match manual computation; over-budget signal
   correct; chart renders and matches the listed values.
3. **Packing:** toggle persistence across reload; template flows add correct
   items; progress accurate; quantity steppers bounded.
4. **Ownership isolation:** User B cannot touch User A's expenses or packing
   items via UI and via direct action invocation.
5. **RLS** if dev Supabase available; else explicit DEFERRED.
6. **UI quality:** states, keyboard, mobile, console, reduced-motion.
7. Fix critical+major defects now; re-verify; record minor defects.

## Acceptance criteria
- Zero open critical/major defects; four commands green; deferred list explicit.

## Verification
- The four commands + devtools pass as described.

Update `tasks/README.md` segment 04.5 (mark Phase 04 complete) when done.
